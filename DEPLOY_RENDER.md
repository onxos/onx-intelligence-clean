# Render Deployment

1. Connect repository to Render Web Service.
2. Use Docker environment with Dockerfile at project root.
3. Ensure env vars exist: OPENAI_API_KEY, OWNER_UNION_ID, APP_ID, APP_SECRET.
4. Deploy latest commit.
