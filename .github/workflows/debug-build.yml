name: Debug Build
on:
  workflow_dispatch:
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: npm ci
        run: npm ci

      - name: vite build
        run: npx vite build

      - name: esbuild api/boot.ts
        run: |
          npx esbuild api/boot.ts \
            --platform=node \
            --bundle \
            --format=esm \
            --outdir=dist \
            --tsconfig=tsconfig.server.json \
            --banner:js="import{createRequire}from'module';const require=createRequire(import.meta.url);"

      - name: Check dist output
        run: ls -lh dist/

      - name: Test runtime startup
        run: |
          echo "=== Starting server with trace flags ==="
          node --trace-uncaught --trace-warnings dist/boot.js \
            2>&1 &
          SERVER_PID=$!
          echo "Server PID: $SERVER_PID"
          sleep 4
          echo "--- server log after 4s ---"
          curl -sf http://localhost:10000/api/trpc/health.ping || echo "HEALTH_CHECK_FAILED"
          wait $SERVER_PID 2>/dev/null; echo "Exit code: $?"
        env:
          NODE_ENV: production
          PORT: "10000"
          DATABASE_URL: "sqlite:///tmp/test.db"
          APP_ID: test
          APP_SECRET: test
          KIMI_AUTH_URL: https://auth.kimi.com
          KIMI_OPEN_URL: https://open.kimi.com
