# دستور السرعة — ONX SPEED CONSTITUTION v1.0
**التاريخ:** 2026-07-09 | **الحالة:** ملزم (L1 — تحت المبادئ السبعة مباشرة) | **المرجع:** MED v3.0 §4.3 (المكدس المقفول)

> **مبدأ المؤسس الحاكم:** «نظام مع كل ضخامته وقوته وقدراته أسرع وأخف من أسرع نظام ضخم
> بالعالم، وأسرع من أصغر نظام من صفحة واحدة.»
>
> هذا الدستور يحوّل المبدأ إلى **ميزانيات مقاسة ملزمة** — أي تجاوز لها عيب دستوري يُسجَّل
> ويُعالج كأولوية، بنفس صرامة معادلة OCMBR (MED §7.1).

---

## المادة 1 — الميزانيات الملزمة (Performance Budgets)

| المؤشر | الميزانية | طريقة القياس |
|---|---|---|
| حساب الخادم لكل طلب API (p95) | **< 100ms** | ttfb − tls من نقطة قريبة للخادم، أو server-timing |
| وصول من السعودية (TTFB p95) | **< 150ms** | curl من جهاز المؤسس |
| فتح أي شاشة (LCP) | **< 1s** | فحص متصفح |
| حزمة JS لأي شاشة | **< 250KB** مضغوطة | next build output |
| أي استعلام قاعدة بيانات (p95) | **< 20ms** | pg_stat_statements |
| نمو البيانات لا يغيّر الميزانيات | مليون سجل = نفس الأرقام | اختبار حمل دوري |

**قاعدة القياس قبل الادعاء:** لا يُعلن أي تحسين سرعة إلا برقم قبل/بعد قابل لإعادة القياس.

---

## المادة 2 — القرارات المعمارية المقفولة (لماذا نحن أسرع بنيةً)

1. **Monolith معياري لا microservices** — قفزة شبكية واحدة لكل طلب. أنظمة SAP/Oracle تدفع
   ضريبة 5-15 قفزة داخلية لكل شاشة؛ نحن قفزة واحدة. هذا القرار **مقفول** ولا يُفتح إلا بـCCR-A.
2. **الضخامة في القاعدة لا في الطلب** — 716 جدولاً و195 نظاماً لا تعني شيئاً لزمن الطلب الواحد
   إذا حمّلت كل شاشة شريحتها فقط (code-splitting عبر App Router — قائم) وسأل كل استعلام
   جدوله المفهرس فقط.
3. **الأحداث خلفية لا أمامية** — حين يُبنى الجهاز العصبي (المرحلة أ)، النشر في outbox
   لا يجوز أن يضيف > 5ms للطلب الأمامي؛ التسليم دوماً غير متزامن.

---

## المادة 3 — خط الأساس المقاس (2026-07-09، قبل الإصلاحات)

قياس حي من جهاز المؤسس (السعودية) → Render فرانكفورت:

| القياس | القيمة | التفكيك |
|---|---|---|
| /api/health (162B) | ttfb 661ms | dns 71 + tcp 180 + tls 522 + حساب ~140 |
| / (الرئيسية، دافئة) | ttfb 482ms | — |
| /systems/narcoticsEntry | ttfb 484ms | — |
| /patients | ttfb 564ms | — |
| ملف ثابت favicon | ttfb 495ms | يُحسب في كل مرة — لا cache |

**الاستنتاج المفصلي:** حساب الخادم الحقيقي ≈ **140ms** فقط؛ ~70% من الزمن المحسوس
سفرُ حزم (الرياض↔فرانكفورت ≈ 110ms ذهاب × 4-5 رحلات لإتمام TLS+HTTP).
**السرعة معركة جغرافيا أولاً، ثم فهارس، ثم كاش.**

**تحديث تنفيذي (نفس اليوم):** S1 منفّذ ومتحقق — **329 فهرساً مركّباً** `ix_<table>_brc`
أُنشئت عبر job-d97mu47avr4c73d7lg60 وتم التحقق بعدّها في pg_indexes (job-d97muu67r5hc73d2ao80).

---

## المادة 3-مكرر — قياس ما بعد الحافة (2026-07-09، S2 منفّذ)

**البنية المنفّذة:** onxvet.com → Route 53 (ALIAS) → CloudFront `E30H7T19L10GQ8`
(سعر عالمي، HTTP/2+3، شهادة ACM) → Render فرانكفورت.
السلوك الافتراضي: تمرير كامل بلا كاش (CachingDisabled + AllViewerExceptHostHeader —
الكوكيز والجلسات تمر سليمة)؛ مسار `/_next/static/*`: كاش حافي كامل (CachingOptimized).

قياس حي من نفس الجهاز (السعودية) عبر onxvet.com:

| القياس | قبل (مباشر لفرانكفورت) | بعد (عبر الحافة) | التحسّن |
|---|---|---|---|
| tcp connect | 180ms | **80-90ms** | −50% (المصافحة مع حافة قريبة) |
| tls | 522ms | **260-410ms** | −30~50% |
| / (دافئة) ttfb | 482-500ms | **584→664→727→~600ms متوسط** ديناميكي غير مكيَّش | يمر للأصل — التوفير في TCP/TLS فقط |
| /dashboard (مصادَق) ttfb | ~500ms | **399ms** | −20% |
| أصل ثابت `/_next/static/*.js` | 495ms (يُحسب كل مرة) | **204ms و`X-Cache: Hit from cloudfront`** | −59% + لا يلمس الخادم إطلاقاً |
| تسجيل الدخول + كوكيز عبر الحافة | — | **200 + جلسة تعمل** | ✅ لا كسر مصادقة |

**قراءة أمينة للأرقام:** المكسب الأكبر الآن في *الأصول الثابتة* (كل JS/CSS يُخدم من
حافة سعودية بكاش سنة كاملة) وفي مصافحات TCP/TLS. الصفحات الديناميكية ما زالت تسافر
لفرانكفورت لأننا اخترنا عمداً عدم كيّشها (صحة الجلسات أولاً). خفضها تحت 150ms يتطلب
S4 (كاش قراءة) أو حساباً حافياً — مرحلة تالية بقرار منفصل.

**التراجع الآمن:** إعادة سجل A في Route 53 إلى `216.24.57.1` تعيد المسار المباشر خلال دقائق.

---

## المادة 4 — برنامج الإصلاح (بترتيب الأثر)

| # | الإجراء | الأثر المتوقع | الحالة |
|---|---|---|---|
| S1 | فهارس ساخنة: مركّب (branch_id, created_at DESC) على كل جدول أعمال | يحفظ الميزانية عند نمو البيانات من اليوم الأول | ✅ منفّذ (job-d97mu47avr4c73d7lg60) |
| S2 | حافة/CDN أمام المنصة (CloudFront + onxvet.com) — يشمل خدمة الأصول الثابتة من الحافة | −300~400ms لكل طلب من السعودية — أكبر مكسب منفرد | ✅ منفّذ ومقاس (المادة 3-مكرر) |
| S3 | ~~كاش CDN عبر إعداد Render~~ **تصحيح (2026-07-09):** إعداد `cache.profile` في Render هو كاش *البناء* لا CDN — لا يوجد CDN لخدمات الويب في Render؛ الكاش الحافي يتحقق حصراً عبر S2 | دُمج في S2 | ✅ مصحَّح |
| S4 | كاش قراءة (Redis أو LRU داخلي) للقوائم كثيفة الطلب | −50~100ms على الشاشات الساخنة | مرحلة تالية |
| S5 | pg_stat_statements + لوحة مراقبة الميزانيات | إنفاذ المادة 1 آلياً | مرحلة تالية |
| S6 | اختبار حمل بمليون سجل مصطنع على 5 أنظمة رائدة | برهان «النمو لا يبطئ» | قبل أي إطلاق تجاري |

---

## المادة 5 — قواعد الكتابة الملزمة لكل كود قادم

1. كل `getAll` يُقيَّد بـ limit (سقف 100) — قائم في المصنع، يبقى قانوناً.
2. لا `SELECT *` في المسارات الساخنة حين تكبر الجداول — أعمدة القائمة فقط.
3. كل عمود يظهر في WHERE أو ORDER BY لجدول أعمال ⇒ مفهرس.
4. كل mutation تنشر حدثاً ⇒ النشر إدراج واحد غير متزامن (< 5ms).
5. أي صفحة جديدة تُقاس قبل الدمج: حجم حزمتها + LCP.

---

*يُراجع هذا الدستور كل ربع سنة أو عند كل تغيير بنيوي (CCR). القياسات المرجعية تُعاد
بعد كل إجراء من المادة 4 وتُلحق هنا.*
