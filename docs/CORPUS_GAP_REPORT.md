# تقرير فجوة الكوربوس المعرفي نحو أرضية 25,000 — STE-N-01

**التاريخ:** 2026-07-13 · **الفرع:** onxos-ste01-deploy-readiness · **المنهج:** عدّ مباشر + بصمات SHA-256 (حتمي، بلا LLM، بلا DB خارجي)

بلغة الحقيقة الخماسية: **موجود** / **جزئي** / **مفقود** / **مدّعى بلا دليل** / **متضارب**.

## 1) جرد المصادر بالعدّ المباشر

| المصدر | الحالة | الدليل | العدّ الفعلي |
|---|---|---|---|
| أرشيف المقالات «19,012 سجلاً» (onx-database.db 9MB) | **مفقود من هذا المستودع** | لا ملف `.db` في الشجرة؛ الذكر الوحيد إشارة وثائقية لريبو آخر onx-platform-complete: `docs/ONX_MASTER_EXECUTION_DOCUMENT_v3.0.md:22,158`، `docs/ONX_UNIFICATION_PLAN.md:27` | 0 هنا |
| `knowledge-seed-15k.json` (11MB) | **مفقود** | `scripts/seed-corpus.ts:13` يبحث عنه ولا يجده؛ بحث شامل بالشجرة = لا نتيجة | 0 هنا |
| fallback المدمج في `scripts/seed-corpus.ts:144-177` | **موجود** | 20 موضوعاً بيطرياً × 5 قوالب | **100** سجل |
| `seedKnowledge()` في `api/knowledge-router.ts` (بذر الذاكرة عند الإقلاع) | **موجود — اصطناعي قالبي** | `api/knowledge-router.ts:126-187`؛ مجموع recordCount للنطاقات الـ19 (`api/knowledge-router.ts:76-94`) | **22,500** سجل |
| كوربوس SBP (131 وثيقة) + أصول المؤسس (305) | **مفقود — بشهادة وثائق الريبو نفسها** | `docs/ONX_MASTER_EXECUTION_DOCUMENT_v3.0.md:123` («0% منها في أي مستودع»)، `docs/ONX_AUDIT_REPORT_2026.md:139` («يتيمة») | 0 هنا |
| `archive/` | **جزئي (manifest فقط)** | ملف وحيد `archive/pre-alpha/ARCHIVE_MANIFEST.md` (832 بايت) — سجل تصفير بيانات، لا بيانات | 0 |

## 2) manifest إزالة التكرار (SHA-256 للمحتوى المطبّع title+body)

- الأداة: `scripts/corpus-dedup-manifest.ts` + التصديرات `normalizeKnowledgeText` / `fingerprintKnowledge` / `buildCorpusManifest` في `api/knowledge-router.ts:~160-199`.
- الاختبار القابل للتكرار: `api/__tests__/corpus-manifest.test.ts` (4 اختبارات، بلا DB).

**النتيجة المقاسة (مثبتة بتوكيدات الاختبار):**

| المقياس | القيمة |
|---|---|
| الإجمالي الخام في المخزن | **22,500** |
| الفريد ببصمة title+body | **22,500** |
| الفريد بالعنوان فقط | **22,500** |
| التكرارات | **0** |
| النطاقات | 19 |

لا تكرار بصمات — لكن العناوين فريدة **بالبناء** (`topic — domain N`): التنوع الدلالي الحقيقي = **190 موضوعاً** فقط (10 قوالب × 19 نطاقاً) يتكرر بترقيم. **الفرادة البصمية ≠ معرفة أصيلة.**

## 3) القياس الفعلي للمخزن (الرقم الحقيقي لا الادعاء)

- المقاس: **22,500** سجلاً في الذاكرة، يُعاد توليدها عند كل إقلاع (**لا ثبات** — Map في `api/knowledge-router.ts:114`).
- الادعاءات المتضاربة:
  - `CERTIFICATION.md:30`: «25,000 / 25,000 = 100%» — **مدّعى بلا دليل** (المقاس 22,500).
  - `README.md:39`: «25,000» — **متضارب**.
  - `api/knowledge-router.ts:372` (`stats`): يقيس ضد هدف 15,000 (`seedProgress: 22500/15000`) — **متضارب داخلياً**.

## 4) الفجوة نحو أرضية 25,000 (قرار D-6)

- بحساب البصمات فقط: 25,000 − 22,500 = **2,500** وحدة ناقصة.
- بحساب المعرفة الأصيلة: الـ22,500 قالبية اصطناعية ⇒ **الفجوة الفعلية ≈ 25,000 كاملة**.

**مصادر السد المقترحة (بالترتيب):**
1. استرداد `onx-database.db` (19,012 سجلاً) و`knowledge-seed-15k.json` من مستودع onx-platform-complete — غير متاح لهذه الجلسة (مهمة استرداد منسّق).
2. إيداع أصول SBP الـ131 + الكابستونات الـ33 (305 أصلاً، 0% مودعة حالياً).
3. تمرير أي مصدر جديد عبر خط `fingerprintKnowledge` الجاهز الآن لضمان dedup قابل للتكرار قبل البذر.

## 5) إثبات خط الأنابيب محلياً

`api/__tests__/corpus-manifest.test.ts`: ثبات التطبيع (مسافات/حالة أحرف) · عيّنة ingest فيها تكرارات تنهار 4→3 · manifest كامل 22,500/22,500/0/19 — كلها خضراء ضمن الحزمة الكاملة (890 ناجحاً / 0 فاشل).
